name: smart_emv_example
description: "Demonstrates how to use the smart_emv package."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  smart_emv:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
